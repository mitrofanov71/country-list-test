const initialState = {
    list: [],
    selectedCountry: null,
}

export const countryListReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}